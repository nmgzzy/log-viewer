[package]
name = "lv-app"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Log viewer desktop application (egui)"

[[bin]]
name = "logviewer"
path = "src/main.rs"

[dependencies]
lv-core = { path = "../lv-core" }
anyhow = { workspace = true }
